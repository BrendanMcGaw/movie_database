// import { express } from "../server";
const express = require("express");
// Think I'll need this for the json middleware when I grab data at some point. Will wait and see.
const router = express.Router();
// Create a route for my moviePage site so that I can Link to the page from my route?
module.exports = router;
